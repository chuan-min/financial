<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cm.dao.UserDao">
    <!--save -->
<!--    <insert id="save" parameterType="User">-->
<!--        insert into t_user values(#{id},#{name},#{age},#{bir})-->
<!--    </insert>-->
    <!--findAll-->
    <select id="findAll" resultType="Fm">
        select * from fm
    </select>
<!--addFm-->
    <insert id="addFm" parameterType="com.cm.entity.Fm">
        insert into fm (fmname, fmpwd, fmsex,fmimg) values (#{fmname}, #{fmpwd}, #{fmsex}, #{fmimg})
    </insert>
</mapper>